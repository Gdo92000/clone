/**
 * Migrations reference.
 *
 * All Drizzle migrations live at the project root in `drizzle/`.
 * This directory exists to provide a clear entry point for migration
 * tooling and to document the migration workflow.
 *
 * To generate a new migration:
 *   npx drizzle-kit generate:pg -- schema src/infrastructure/postgres/schema/*.ts
 *
 * To apply migrations:
 *   npx drizzle-kit push:pg -- connectionString "postgres://..."
 *
 * Existing migration files: ../../drizzle/
 */
export {};
