# Skills Template Library

This directory contains **reference-only** skill templates. These are not used directly at runtime.

**Do not load skills from this location.** Runtime skills are loaded from .agent/skills/.

This library serves as a source of truth for skill definitions that can be copied to the agent's skill directory when needed.

Last updated: Phase 2 cleanup.
